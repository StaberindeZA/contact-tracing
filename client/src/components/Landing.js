import React from 'react';

// Landing page
// Used to be the primary page, but moved everything into "Main"
// Do we still have a use for a landing page?

class Landing extends React.Component {
  render() {
    return (
      <div>
        <h1>The Landing</h1>
        <div>
          What should we do with this page?
        </div>
      </div>
    );
  };
}

export default Landing;